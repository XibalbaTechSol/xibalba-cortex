# Xibalba Graph Memory — Audit Status

Status: AUDIT IN PROGRESS · 2026-08-06

The consolidated cross-repository implementation plan is `/home/xibalba/Documents/INTEGRITY — Cross-Repository Audit and Implementation Plan.md`.

## Verified state

- Local repository: `/home/xibalba/Projects/xibalba-graph-memory`.
- Branch: `main...origin/main [ahead 3]`.
- The worktree contains uncommitted runtime adapters, controller/session synchronization, tests, and a viewer.
- `uv sync --extra drive && uv run pytest -q`: passed.
- Plain `uv sync && uv run pytest -q`: failed during collection because Drive tests import optional Google dependencies without the Drive extra.
- The v1 specification defines the canonical SQLite/provenance/hash-chain/entity/relation/MCP model and correctly separates local evidence from the Integrity Memory Directed Acyclic Graph.

## Tasks

- [ ] Decide whether Drive dependencies are a supported default or optional test group; make the default test command deterministic.
- [ ] Review and commit or discard the uncommitted runtime adapter, controller, session, test, and viewer changes as a separate change set.
- [ ] Reconcile the v1 specification, root SPECIFICATION.md, architecture records, archived plans, and implementation symbols.
- [ ] Expand README with installation, status, privacy, retention, backup/restore, profile isolation, and MCP operations.
- [ ] Verify MCP discovery and direct tool calls through an isolated Hermes profile.
- [ ] Verify Integrity DAG links distinguish byte lineage from truth, authorization, and completeness.
- [ ] Add CI with the correct package extras.

## Production posture

This is a local profile-isolated memory-service prototype with a strong normative model and a passing test suite under the correct extras. It is not production-certified.
