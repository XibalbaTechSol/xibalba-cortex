# Xibalba Cortex — Audit Status

Status: AUDIT IN PROGRESS · 2026-08-06

The consolidated cross-repository implementation plan is `/home/xibalba/Documents/INTEGRITY — Cross-Repository Audit and Implementation Plan.md`.

## Verified state

- Local repository: `/home/xibalba/Projects/xibalba-cortex`.
- Branch: `main...origin/main [ahead 3]`.
- The worktree contains uncommitted runtime adapters, controller/session synchronization, tests, and a viewer.
- `uv run pytest -q`: passed with the opt-in live Hermes smoke skipped by default.
- `cd viewer && npm run build`: passed.
- `XIBALBA_RUN_HERMES_MCP_SMOKE=1 uv run pytest tests/test_hermes_mcp_smoke.py -q`: passed.
- `hermes mcp test xibalba_cortex`: passed; Hermes discovered 51 tools.
- `uv run xibalba-cortex-operator status`: passed; live store reported `integrity_check=ok`.
- Drive ingestion imports optional Google/PDF dependencies lazily, so plain test collection no longer depends on the Drive extra.
- The v1 specification defines the canonical SQLite/provenance/hash-chain/entity/relation/MCP model and correctly separates local evidence from the Integrity Memory Directed Acyclic Graph.

## Tasks

- [x] Decide whether Drive dependencies are a supported default or optional test group; Drive remains optional and import-safe without the extra.
- [ ] Review and commit or discard the uncommitted runtime adapter, controller, session, test, and viewer changes as a separate change set.
- [ ] Reconcile the v1 specification, root SPECIFICATION.md, architecture records, archived plans, and implementation symbols.
- [x] Expand README with installation, status, privacy, retention, backup/restore, profile isolation, and MCP operations.
- [x] Verify MCP discovery through the configured Hermes profile.
- [x] Verify Integrity DAG links distinguish byte lineage from truth, authorization, and completeness.
- [x] Add CI command guidance with the correct package extras in README.md.

## Production posture

This is a local profile-isolated memory-service prototype with a strong normative model and explicit optional Drive dependency policy. It is not production-certified.
